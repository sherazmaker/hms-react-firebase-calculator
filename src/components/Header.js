import React from "react";

const Header = () => {
    return (
        <div>
            <h1 style={{ color: "white" }}>First Name, Last Name, Logout</h1>
        </div>
    );
};

export default Header;